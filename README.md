This is a package for multi-resolution HEALPix maps of the gravitational wave background with pulsar timing array data.

You can install it using pip:
```
pip install resolve-gw
```

See the example notebooks for tutorials on how to use the sampler and post-process the results or how to perform information-criterion-based model selection.